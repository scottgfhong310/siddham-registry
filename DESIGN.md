# siddham-registry — 設計決議

> README 管「怎麼用」，本檔管「**為什麼長這樣**」。家族共同規範只標引用不重複
> （<https://github.com/scottgfhong310/nodeapp-webapp-family>）。
> 資料層的權威宣告在 `db_siddham` 的治理文件（private repo `siddham-maintenance`）。

---

## 1. ⚠️ 字型：為什麼這個 app 不附 CBETA 悉曇字型

**沒有附，是因為沒有授權可以附。** 這不是體積或偏好的決定。

查證（2026-08-17，一手）：

| 來源 | 說了什麼 |
| --- | --- |
| `Siddam.ttf`／`.otf` v1.062（2023） | 字型內 **0 個** Copyright／Trademark／License／LicenseURL 欄位 |
| **v2.00（2015）**〔現行登錄版本〕 | Copyright 欄只有 `CBETA` 四個字，**0 個** License／LicenseURL／Trademark 欄位（2026-08-17 逐項複查） |
| CBETA 下載頁 | 對這個檔案**沒有任何授權條款**，只說明怎麼安裝 |
| CBETA 站台 | CC BY-NC-SA 3.0 TW——那是**網站**的授權，且為 **NC**，未言明涵蓋字型二進位 |
| `NotoSansSiddham-Regular.woff2` | name13 = **SIL OFL 1.1**、name14 = 授權 URL |

**兩個容易讀反的地方：**

1. **`fsType = 0x0000「Installable Embedding」不是散布許可。** 那一欄回答的是
   「能不能把字型**嵌進文件／PDF**」。查過的 CBETA 字型都是 0，而那**不構成**散布權。
   ⚠️ 現行登錄的 v2.00 **出處比 v1.062 更弱**（它不是 CBETA 下載頁現在提供的那一份），
   所以改判權威版**沒有讓這一條變寬鬆**。
2. **沒有授權條款 ≠ 可以用。** 著作權預設保留，要有**明示授予**才有散布權。

**對照本身就是證據**：可散布的字型會把授權寫在自己身上（Noto 有 name13／name14），
那三支沒有。**一個著作權人姓名不是一份授權。**

### 做法

- CBETA 造字：`@font-face { font-family: 'CbetaSiddam'; src: local('Siddam'); }`
  ——**刻意沒有 `url()` 退路**，加了就等於把字型放進 repo。
  這正是 CBETA 自己的散布方式：它的下載頁就是叫人裝進系統字型。
- Unicode Siddham：**bundle `Noto Sans Siddham`**（OFL，附 `OFL.txt`）。
- 偵測不到就顯示說明 ＋ CBETA 下載連結，**不留白**（家族 §11.1）。

### 這個決定犧牲了什麼（量過的）

- 資料裡用到 **75 個**悉曇碼位，Noto **缺 0 個** → Unicode Siddham 那一半**完整**。
- **反查完全不需要字型**（貼造字文獻 → 對回 unicode siddham）。
- 真正需要本機字型的只有「43 個 face 的書法字形長什麼樣」那一段。

### ⚠️ 一個沒辦法消除的不確定

**使用者看到的是他自己裝的那一版，我們無法保證是哪一份。** 實測同一個家族名 `Siddam`
至少有兩份不同的檔在流通，而它們畫得出來的字不一樣：

| 版本 | 不在 cmap | **空字形** | 合計畫不出來 |
| --- | --- | --- | --- |
| v1.062（2023） | 2 | 62 | **64** |
| **v2.00（2015）**〔登錄版本〕 | **0** | 28 | **28** |

**「在 cmap 裡」不等於「畫得出來」**——cmap 有對應但字形是空的（無輪廓）時，畫面一樣空白，
而只看 cmap 的涵蓋率檢查會高估。

⚠️ **「無法保證是哪一份」不是修辭，它是機制上的**：兩個建置的 `family`／`full name`／
`PostScript name` **全部都是 `Siddam`**，而 `@font-face` 走 `local('Siddam')` 依**名字**比對
——所以**網頁拿不到版本、也偵測不出來**。`font-probe.js` 能回答的只有「裝了沒有」。

於是**字型說明卡的措辭分成兩種，不可混用**〔2026-08-17〕：

| 這一列的字型 | 印什麼 | 為什麼 |
| --- | --- | --- |
| 隨 app 提供（Noto） | 「**版本** 2.004」 | 登錄版本**就是**你拿到的那一支，說得準 |
| 走 `local()`（Siddam） | 「**登錄版本** 2.00（同名建置不只一個，本頁分不出你裝的是哪一支）」 | 印成「版本 2.00」等於**替使用者宣稱一件本頁查不到的事** |

**權威版於 2026-08-17 由 v1.062 改判為 v2.00**〔owner；判準由「哪一份是 CBETA 現行釋出」
改為「哪一份畫得出這份登錄」〕，`db_siddham.tb_font` 因此 3 → 2 列
（v2.00 沒有 OpenType 版，`siddam-otf` 退場）。理由與訂正見上游治理文件 §9.4。

---

## 2. ⚠️ 三種「沒有」意思不同，第四種最容易寫錯

這是本 app 最容易寫壞的地方。四種狀態在資料上長得很像，而只有分開講才是誠實的：

| 狀態 | 資料 | 實查 | 意思 |
| --- | --- | --- | --- |
| 字形沒有音 | `glyph.syl === null` | 456 | 群組／筆順／咒輪／文章記號**本來就沒有讀音** |
| 沒有登錄組字 | `composition[i] === null` | 468 | 來源沒有記這個字怎麼組 |
| **基本字** | `composition[i] === [i]` | **57** | 來源說它**拆到自己為止**（子音／母音／virama…） |
| 空位 | 元素為 `-1` | 21 | 來源以 `〇` 標示的空缺 |

⚠️ **把第 3 種顯示成「一個組件的組字」是錯的**，把它併進第 2 種更錯——
那會讓「來源說它是基本字」看起來像「這筆資料壞了」。明細卡三種各講一句不同的話。

⚠️ **反查同理**：「不在登錄裡」（可能根本不是造字）與「在登錄裡但沒有音」是兩件事，
併成「找不到」就把資訊弄丟了。

---

## 3. 為什麼是一支雙頁 app，不是兩個 repo

需求分成兩部分（字形對照／悉曇十八章），而資料層實查也確實是**兩批**
（face 36 的 6,442 列 vs 其餘 36 個 face 的 2,624 列，unicode 串交集只有 309 種）。
但它們共用同一份產物、同一張明細卡、同一套字型與偵測邏輯——分兩個 repo 就是兩份複製件。
家族已有四支雙頁 app 的先例（FC／CDA／COPIC／finecolour 的 `sets.html`）。

明細卡與字型偵測因此是**跨頁模組**（`glyph-detail.js`／`font-probe.js`）：
碰 DOM 所以進不了 `-lib.js`，跨頁共用所以不該在兩個控制器各抄一份（§4.1 的第三種模組）。

---

## 4. 章名的 `<sub>`：與「一律 escape」正面相撞

來源的章名自帶標記（`迦迦<sub>(引)</sub>章`，18 章有 **7 章**）。

- 整段跳脫 → 畫面印出字面的 `<sub>`；
- 整段信任 → 把資料當 markup 用，違反家族 §8。

做法是**只放行 `<sub>`**：先整段 `esc()`，再把跳脫後的 `&lt;sub&gt;` 換回真標籤。
`verify.js` 第 ⑦ 條釘著這三件事都在。

---

## 5. ⚠️ 字型偵測：三種量法都不成立，第四種才對

修這個 bug 時連續量錯三次，值得寫下來：

| 量法 | 為什麼不成立 |
| --- | --- |
| `measureText` 比寬度 | 載體字元是 **CJK 表意文字，在任何字型裡都恰好 1em**（實測兩邊都 100.0px）。寬度永遠相同 ⇒ **必然回報「沒裝」**，而畫面上字型好好地在作用 |
| `document.fonts.check()` | 實測對每一支候選都回 `true`——它答的是「這個字型在不在」，不是「它畫不畫得出這個字」 |
| 拿沒安裝的候選字型比對 | 等於沒測（會退回同一個後備） |

**正解是量像素**：同一個字分別用目標字型與純後備畫進 canvas，比對墨跡。
並且**自帶對照組**——拿一個不存在的字型再畫一次，它必須與純後備完全相同；
不相同就表示探針本身不可信，一律回報 `null`（＝不知道）而不是猜一個答案。
⚠️ **`null` 不掛告示**：探針不可信時掛一張「你沒裝字型」可能是在說謊，沉默至少只是少講一句話。

---

## 6. ⚠️ 羅馬轉寫要自己的字型堆疊

`fd_latin` 是 **ISO 15919**（不是 IAST——實測與 `bonji` 引擎相符率 IAST 88.55% vs ISO 99.59%），
而 ISO 15919 用**組合附加符號**寫鼻化與成音節流音。資料裡實測四種：

`U+0310` candrabindu **28 個音節**／`U+0325` 環下 151／`U+0304` macron 36／`U+0332` 底線 25。

介面字型（`-apple-system`）與泛型 `serif` **畫不出 U+0310**，`am̐` 會變成 `am` ＋ 豆腐方塊。
故羅馬那幾個元素單獨用 `--latin` 堆疊（Charis SIL／Gentium Plus／Times New Roman／
Arial Unicode MS／Helvetica，實測畫得出來）。
⚠️ 一台機器上一支都沒有時仍會是豆腐——那影響 28/6,898，而**記法（`a~m`）永遠顯示在旁邊**，
識別不會因此遺失。

---

## 7. 資料的形狀（為什麼產物長這樣）

- **欄序寫在產物裡**（`SID_*_COLS`），消費端依名字解包——否則匯出器欄序一改就靜默錯位。
- **組字是與 `SID_GLYPHS` 等長的平行陣列**，不是 `{glyph: [...]}` 的物件：26,532 個組件，
  平行陣列省掉 8,598 個鍵。`lib.build()` 會斷言兩者等長，不等長就丟例外**而不是半套跑下去**。
- **`fd_idx` 永不出現在產物裡**（治理 §5 規則 1）——只有自然鍵與陣列位置。
- 六個產物分檔：字形對照頁載五個、十八章頁載六個（多一個 `siddham-chapters.js` 97 KB）。

---

## 8. 契約檢查（`scripts/verify.js`）

15 條，`--selftest` 18 個注入逐一確認**為了對的理由紅**。其中第 ①②③ 條是**授權**——
家族其他成員沒有這一類檢查，因為只有這支 app 面對「一個顯示得好好的字型，
但它不該在這裡」的情況。**它壞掉時畫面完全正常。**

### 8.1 第 ⑮ 條：明細卡的 Modal 實例不可被第二次 init 換掉

**症狀是「卡片開得起來、關不掉」，而畫面上一切正常。** 成因：Materialize 的
`M.Modal.init` 對同一個元素跑第二次會先 `destroy()` 舊實例（連同它的 click／keydown
handler）再建一個新的，**而握著舊實例的人不會收到任何通知**。
明細卡是 `glyph-detail.js` 自己 init 的；`siddham-registry.js` 又寫了
`M.Modal.init(document.querySelectorAll('.modal'))`（為了 `#font-modal`），於是：

- 開卡走的是**已被 destroy 的**那個實例——`open()` 只是加 `open` class ＋ 貼 overlay，照樣有效；
- 活著的那個實例 `isOpen` 恆為 `false`，而 `close()` 第一行就是 `if (this.isOpen === false) return;`
  ⇒ **「關閉」／點遮罩／ESC 三條路全部早退**。

⚠️ **對照組就在同一個 repo 裡**：`chapters.html` 用的是同一支 `glyph-detail.js`，
但 `chapters.js` **沒有**那行掃全部的 init——所以它一直是好的。
兩頁行為不同而程式碼共用時，差異一定在**呼叫端**。

修法兩層，缺一不可：

1. 控制器**只 init 自己那一個 modal**（`$('font-modal')`），不掃 `.modal`；
2. `glyph-detail.js` 開卡時才用 `M.Modal.getInstance()` 要**當下**的實例，
   **不把 init() 的回傳值存成模組變數**。它是兩頁共用的模組，管不到呼叫端會不會再
   init 一次——**與其正確處理「實例被換掉」的每一種情況，不如讓它不成立**。

第 ⑮ 條兩層各驗一半，三個注入分別對應「控制器掃全部」「快取實例」「不問當下實例」，
**三者失敗訊息各不相同**（兩個注入報同一句話，等於只有一個注入）。

⚠️ `--selftest` 的注入會先斷言**真的改到東西**——一個什麼都沒改到的注入，
與一條失效的檢查，在輸出上長得一模一樣。（第 ⑬ 條就是這樣抓到自己太鬆的：
`/SID_GLYPH_COLS/` 也會命中被改名成 `SID_GLYPH_COLS_X` 的宣告。）

---

*MIT © 2026 Scott G.F. Hong*
